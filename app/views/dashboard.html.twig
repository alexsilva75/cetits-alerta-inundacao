
    {% extends 'layouts/dashboard.base.html.twig' %}
    {% block content %}
        <h1>Dashboard</h1>

        
        {% if session.user %}
            <p>Bem-vindo, {{ session.user.getNome() }}!</p>

        {% endif %}

    {% endblock %}
